// note that by default everything will be assumed to be righty. you MUST indicate in your data that an alg is lefty if it is.
// assume starting rotation or algvis to be a righty view, the code will flip it for any algs marked lefty.
// UNCOMMENT AND FILL THE BELOW CODE

import type { IAlgset, ICaseList } from "../../../types/dataFormat"
// import { useGrouping } from "../configs/setDefinition/setGroupings/X";

// import yml from '../data/X.yml' // REPLACE WITH THE SET YAML
// const data: ICaseList = yml // just to typecheck

// export const XSheetGroupings: IAlgset = useGrouping('none', data) // add any views to export here
