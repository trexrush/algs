---
import VerticalLayout from "../../layouts/VerticalLayout.astro";
import Navbar from "../../layouts/Navbar.astro";
import { algSet } from "../../templates/zbll";
import { sheetURL } from "../../templates/url";
import Page from "../../layouts/Page.astro";

const response = await fetch(sheetURL + "?name=L_ZBLL");
const sets = await response.json();
---

<Page title={'L_ZBLL'}>
  <Navbar slot="navbar" />
  <VerticalLayout options={algSet.options} {sets} />
</Page>
