---
import { algSet } from "../../templates/zbll";
import VerticalLayout from "../../layouts/VerticalLayout.svelte";
import { sheetURL } from "../../templates/url";
import Navbar from "../../layouts/Navbar.astro";

const response = await fetch(sheetURL + "?name=T_ZBLL")
const sets = await response.json()
---

<main>
  <Navbar title={'T ZBLL'}>
    <VerticalLayout client:idle data={algSet.data} {sets} />
  </Navbar>
</main>

<style></style>